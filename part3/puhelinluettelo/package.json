{
  "name": "puhelinluettelo",
  "version": "1.0.0",
  "dependencies": {
    "express": "^5.2.1",
    "morgan": "^1.11.0"
  },
  "scripts": {
    "start": "node index.js",
    "build": "npm install --prefix ../../part2/puhelinluettelo && npm run build --prefix ../../part2/puhelinluettelo && rm -rf dist && cp -r ../../part2/puhelinluettelo/dist .",
    "build:ui": "npm run build"
  }
}
